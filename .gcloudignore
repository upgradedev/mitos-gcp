.git/
.github/
tests/
__pycache__/
*.pyc
# A .gcloudignore replaces .gitignore rather than adding to it, so these have
# to be named again here or `gcloud builds submit` uploads 116 MB of installed
# packages on every build. The image installs its own from the lockfile and
# builds its own dist.
web/node_modules/
web/dist/
